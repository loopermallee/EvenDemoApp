name: demo_ai_even
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # Networking (existing)
  dio: ^5.4.3+1

  # State management (existing)
  get: ^4.6.6

  # CRC (existing)
  crclib: ^3.0.0

  # Toasts (existing)
  fluttertoast: ^8.2.8

  # NEW: required for ChatGPT BYOK
  http: ^1.2.2
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/